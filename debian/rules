#!/usr/bin/make -f

%:
	dh $@ --buildsystem=cmake --parallel

override_dh_auto_configure:
	dh_auto_configure -- -DCMAKE_BUILD_TYPE=Release

override_dh_shlibdeps:
	dh_shlibdeps -l/usr/arm-linux-gnueabihf/lib:/usr/lib/arm-linux-gnueabihf
